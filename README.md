# omqs-AI-integration-kit
It gives OMQS users a ready-made package they can drop into ChatGPT, Claude, Gemini, Cursor, Windsurf, VS Code, or any coding agent so the model understands not only the API syntax, but also the correct way to reason with OMQS information.
